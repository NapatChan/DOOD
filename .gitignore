# dependencies
node_modules/

# build output (โฮสต์ build เองจาก source)
dist/
*.tsbuildinfo

# ความลับ — สำคัญ! กัน API key หลุด (เฟส 2 AI)
.env
.env.*
!.env.example

# Canva browser profile (เก็บ session/cookie ล็อกอิน — ห้ามขึ้น repo)
.canva-profile/

# ไฟล์ชั่วคราว/ทดสอบ
scratch-*
scratch-shots/

# ระบบ/เครื่องมือ
.DS_Store
*.local
*.bak
.vite/

# Claude Code settings ส่วนตัว (ไม่ควรขึ้น repo)
.claude/settings.local.json
