# DOOD — Planning (Frontend Phase 1)

แอปแต่งตัวแบบเกม แตะเลือกเลเยอร์แล้วปัดซ้าย/ขวาเพื่อเปลี่ยนเสื้อผ้าบนมาสคอต
เน้น **"เข้ามาปัดได้เลย ง่ายสุด สนุกทันที"**

> เอกสารนี้ปรับปรุงจากฉบับแรก: แก้ความขัดแย้งของโมเดลการปัด, เพิ่มเกณฑ์วัดผลที่วัดได้จริง,
> ออกแบบ state ให้ชัดก่อนลงมือ, และเติม edge case / preload / accessibility / การทดสอบที่ฉบับเดิมขาด

---

## 1. หลักการออกแบบ (ยึดตลอดการทำงาน)

- **เข้ามาปัดได้เลย** — เปิดเว็บมาเจอมาสคอตพร้อมเล่นทันที ไม่มีหน้า welcome ไม่มี login ไม่มีตั้งค่า
- **ปัดครั้งเดียวเห็นผลทันที** — ปัดแล้วเปลี่ยนเดี๋ยวนั้น ลื่น ไม่มีโหลด ไม่มีรอ
- **จบในหน้าเดียว** — ไม่มีเมนูซับซ้อน ไม่ต้องข้ามหน้า
- **เข้าใจได้ใน 3 วินาที** — หน้าจอสอนตัวเองผ่านรูปร่างและการขยับ ไม่ต้องมีคำอธิบาย
- **Mobile-first** — ออกแบบสำหรับมือถือเป็นหลัก แต่ต้องเล่นบน desktop ได้ด้วย (ปุ่ม/คีย์บอร์ด)

> เกณฑ์ตัดสินทุกฟีเจอร์: "สิ่งนี้ทำให้เข้ามาปัดได้เลย หรือช้าลง?" ถ้าช้าลง ให้เลื่อนออกไปก่อน

### เกณฑ์วัด "ลื่น" (Definition of Smooth — วัดได้จริง)
- การปัดต้องได้ **60fps** (ไม่มีเฟรมตกระหว่าง drag) — ใช้ transform/opacity เท่านั้น ห้าม animate layout
- ชิ้นเสื้อผ้าต้อง **ติดนิ้ว** ระหว่างลาก (follow finger 1:1) ไม่ใช่รอปล่อยแล้วค่อยขยับ
- ปล่อยนิ้วแล้ว transition เข้าตำแหน่งใหม่ภายใน **200–300ms** ด้วย spring/ease-out
- แตะแล้วมี feedback ภายใน **100ms** (highlight เลเยอร์ทันที)
- ไม่มี layout shift ตอนโหลด (จองพื้นที่มาสคอตไว้ล่วงหน้า)

---

## 2. Tech Stack

- **React** + **TypeScript** — UI
- **Vite** — build tool (เบา เร็ว เหมาะกับ single-page app)
- **Tailwind CSS** — styling
- **Framer Motion** — จัดการ gesture การปัด (`drag`, `dragConstraints`) + spring animation ให้ลื่น
- ยังไม่ใช้: backend, database, auth, routing (เฟสนี้เป็น frontend ล้วน)

> เหตุผลที่ใช้ Framer Motion: `drag` + `onDragEnd` + `animate` ครอบเรื่อง follow-finger,
> velocity-based release และ spring ให้ครบ ลดโค้ด gesture ที่เขียนเองแล้วมัก jank

---

## 3. โมเดลการปัด (Interaction — ข้อตกลงหลัก)

ใช้โมเดล **"แตะเลือกเลเยอร์ก่อน แล้วปัด"** (แก้ความขัดแย้งของฉบับเดิม):

1. **แตะ** ที่เลเยอร์ (หมวก / เสื้อ / กางเกง) เพื่อเลือกว่ากำลังจะแต่งชิ้นไหน → เลเยอร์นั้นมีไฮไลต์
2. **ปัดซ้าย/ขวา** ที่ใดก็ได้บนพื้นที่มาสคอต = เปลี่ยนเฉพาะเลเยอร์ที่เลือกอยู่
3. ค่าเริ่มต้น: เปิดแอปมาเลเยอร์ "เสื้อ" ถูกเลือกไว้ก่อน (เล่นได้ทันทีโดยไม่ต้องแตะ)

### พฤติกรรมขอบ (Edge behavior)
- ปัดสุดหมวดแล้ว **วนกลับ (loop)** ไปตัวแรก/ตัวสุดท้าย — ไม่มีวันตัน
- **ไม่มีตัวเลือก "ไม่ใส่ (none)"** ในเฟสนี้ ทุกเลเยอร์มีชิ้นสวมอยู่เสมอ

---

## 4. State & Data Model (ออกแบบก่อนเขียน)

### 4.1 โครงสร้างข้อมูลสินค้า
```ts
type Category = 'hat' | 'top' | 'pants';

interface ClothingItem {
  id: string;
  category: Category;
  name: string;
  price: number;
  color: string;      // ใช้แทนรูปในเฟสนี้ (บล็อกสี)
  imageUrl?: string;  // เว้นว่างไว้ก่อน เตรียมสลับเป็น PNG โปร่งในอนาคต
}
```
- เตรียมโครงให้เปลี่ยนจาก `color` เป็น `imageUrl` ได้ง่าย — component render แบบ:
  "ถ้ามี imageUrl ใช้รูป ไม่งั้น fallback เป็น color block"

### 4.2 App State (single source of truth)
```ts
interface AppState {
  selectedLayer: Category;                    // เลเยอร์ที่กำลังแต่ง
  currentIndex: Record<Category, number>;     // index ชิ้นปัจจุบันของแต่ละหมวด
  locked: Record<Category, boolean>;          // ล็อกชิ้นไหนไว้บ้าง
}
```
- เฟสนี้ใช้ `useState`/`useReducer` ใน `App.tsx` พอ ยังไม่ต้องใช้ state library
- ราคารวม = คำนวณ (derive) จาก `currentIndex` ทุกหมวด ไม่เก็บซ้ำใน state

### 4.3 ผลของ Lock
- ถ้า `locked[category] === true` การปัดจะข้ามผลกับหมวดนั้น (แม้แตะเลือกไว้ก็ปัดไม่เปลี่ยน หรือปัดไม่ได้ + มี feedback สั่นเบา ๆ)

---

## 5. ฟีเจอร์ Frontend เฟส 1 (เรียงตามความสำคัญ)

### 5.1 แกนปัดเปลี่ยนชุด (Core — ทำก่อน สำคัญสุด)
- แสดงมาสคอตตรงกลางจอ (placeholder รูปสี่เหลี่ยมสีเทา)
- วาง 3 เลเยอร์ทับบนมาสคอต: หมวก (บน) / เสื้อ (กลาง) / กางเกง (ล่าง)
- ช่วงแรกเสื้อผ้าเป็นบล็อกสี (ยังไม่ใช้รูปจริง)
- แตะเลือกเลเยอร์ → ปัดเปลี่ยนชิ้นของเลเยอร์นั้น (ดูข้อ 3)
- การปัดต้องเข้าเกณฑ์ "ลื่น" ในข้อ 1
- แต่ละหมวดมีตัวเลือกเริ่มต้น 4–5 แบบ

### 5.2 เลือกเลเยอร์ที่จะปัด
- แตะเลเยอร์เพื่อเลือกว่ากำลังจะแต่งชิ้นไหน
- เลเยอร์ที่เลือกอยู่มีเส้นขอบ/ไฮไลต์ให้เห็นชัด (feedback < 100ms)
- มีตัวช่วยบอกทิศ: ลูกศร/จุดเล็ก ๆ (dots) บอกว่าปัดได้ + อยู่ชิ้นที่เท่าไรจากทั้งหมด

### 5.3 ล็อกชิ้นที่ชอบ (Lock)
- แตะปุ่มล็อกเพื่อ "ตรึง" ชิ้นที่ชอบไว้ ไม่ให้เปลี่ยนตอนปัด
- มีไอคอนแสดงสถานะล็อก/ปลดล็อกชัดเจน

### 5.4 ปุ่ม "อยากได้ลุคนี้" + ราคารวม
- แสดงราคารวมของทั้งลุค (derive อัตโนมัติเมื่อเปลี่ยนชิ้น)
- ปุ่มแสดงความสนใจ (เฟสนี้แค่กดได้ + toast ยืนยัน ยังไม่ต่อระบบซื้อจริง)

### 5.5 Desktop / Accessibility fallback
- ปุ่มลูกศร ◀ ▶ ข้างมาสคอต (คลิกเปลี่ยนชิ้นได้ ไม่ต้องปัด)
- คีย์บอร์ด: ลูกศรซ้าย/ขวา = เปลี่ยนชิ้น, ลูกศรบน/ล่าง = สลับเลเยอร์
- ปุ่มและเลเยอร์มี `aria-label` พื้นฐาน

---

## 6. การจัดการรูป/โหลด (เตรียมไว้ ไม่ทำเต็มในเฟสนี้)

- เฟสนี้เป็นบล็อกสี → ไม่มีปัญหาโหลด แต่ **ออกแบบ component เผื่อรูป** ตั้งแต่ตอนนี้
- เมื่อสลับเป็น PNG จริง: **preload ชิ้นถัดไป/ก่อนหน้าของหมวดที่เลือก** ล่วงหน้า เพื่อคงหลักการ "ปัดแล้วเปลี่ยนทันที"
- จองขนาดกล่องเลเยอร์ไว้คงที่ (กัน layout shift)

---

## 7. สิ่งที่ยังไม่ทำในเฟสนี้ (เลื่อนไปเฟสหลัง)

- รูปเสื้อผ้าจริง (เฟสนี้ใช้บล็อกสีก่อน)
- ระบบ anchor วางรูปให้สเกลตรง (ทำตอนใส่รูปจริง)
- ตัวเลือก "ไม่ใส่ (none)" ต่อเลเยอร์
- ระบบแท็กแมตช์เสื้อผ้า
- Login / บันทึกลุค / แชร์
- ระบบชำระเงินจริง
- มาสคอตหลายตัว

---

## 8. โครงสร้างโปรเจกต์ (แนะนำ)

```
src/
  components/
    Mascot.tsx          # แสดงมาสคอต + ประกอบ 3 เลเยอร์
    ClothingLayer.tsx   # เลเยอร์ 1 ชั้น: รับ gesture ปัด + render บล็อกสี/รูป
    LayerSelector.tsx   # แตะเลือกเลเยอร์ที่จะแต่ง + แสดงไฮไลต์
    LookBar.tsx         # ราคารวม + ปุ่มอยากได้ลุคนี้
    NavArrows.tsx       # ปุ่มลูกศร desktop/accessibility fallback
  hooks/
    useWardrobe.ts      # จัดการ AppState (selectedLayer/currentIndex/locked) + คำนวณราคา
  data/
    items.ts            # ข้อมูลเสื้อผ้า (mock) แยกตามหมวด
  types/
    index.ts            # ClothingItem, Category, AppState
  App.tsx
  main.tsx
```

---

## 9. ลำดับลงมือ (ทำทีละขั้น ทดสอบทุกขั้น)

1. ตั้งโปรเจกต์ Vite + React + TS + Tailwind ให้รันได้ เห็นหน้าเปล่ามีคำว่า DOOD
2. วาง type + mock data (`items.ts`, `types/`) + hook `useWardrobe` (ยังไม่มี UI ปัด)
3. วางมาสคอต placeholder + 3 เลเยอร์บล็อกสี อ่านจาก state (ยังปัดไม่ได้)
4. เพิ่มการแตะเลือกเลเยอร์ + ไฮไลต์ (ข้อ 5.2)
5. เพิ่ม gesture ปัดด้วย Framer Motion + วนกลับ (loop) + ปรับให้เข้าเกณฑ์ลื่น (ข้อ 1)
6. เพิ่มปุ่มลูกศร + คีย์บอร์ด (ข้อ 5.5) — ให้เทสง่ายบน desktop
7. เพิ่มล็อกชิ้น (ข้อ 5.3)
8. เพิ่มราคารวม + ปุ่มอยากได้ลุคนี้ + toast (ข้อ 5.4)
9. เก็บกวาด UI ให้สะอาด + ทดสอบบนมือถือจริง (ดู checklist ข้อ 10)

> ทำทีละขั้น ทดสอบทุกขั้นก่อนไปต่อ ถ้าปัดยังไม่ลื่นให้แก้จนลื่นก่อนค่อยเพิ่มฟีเจอร์

---

## 10. เกณฑ์ผ่านของเฟส 1 (Acceptance checklist)

- [ ] เปิดเว็บมาแตะ/ปัดเล่นได้ภายใน 3 วินาที ไม่มีหน้าคั่น
- [ ] แตะเลือกเลเยอร์ได้ทั้ง 3 + เห็นไฮไลต์ชัด
- [ ] ปัดเปลี่ยนชิ้นได้ทุกหมวด ลื่น 60fps ติดนิ้ว วนกลับได้
- [ ] ล็อก/ปลดล็อกได้ และล็อกแล้วปัดไม่เปลี่ยนจริง
- [ ] ราคารวมอัปเดตถูกต้องทุกครั้งที่เปลี่ยนชิ้น
- [ ] กดปุ่ม "อยากได้ลุคนี้" แล้วมี feedback
- [ ] เล่นบน desktop ได้ด้วยปุ่มลูกศร/คีย์บอร์ด
- [ ] ทดสอบบนมือถือจริงอย่างน้อย 1 เครื่อง (iOS หรือ Android)

#สิ่งที่ต้องทำต่อ
- ระบบตะกร้า จัดเก็บชุดที่ต้องการ พร้อมลิงค์สินค้าพร้อมรายละเอียดต่างๆ(ถ้าไม่มีลิงค์ให้ขึ้นลิงค์ปลอมๆไปก่อน)
- ปุ่มเปิดปิด หมวก
- AI agent ไว้เอารูปโปรดักมาลบพื้นหลังและปรับขนาดให้เท่ากันใน canva
- AI agent ไว้สำหรับนำรูปมาใส่ในเว็ปและจำแนกประเภท ตั้งชื่อสินค้าได้เอง