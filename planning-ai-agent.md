# DOOD — Planning: AI ผู้ช่วยลงสินค้า + สไตลิสต์

แผนสร้าง "AI ประจำร้าน DOOD" ที่ช่วยตั้งแต่**นำรูปสินค้าดิบเข้าระบบ**ไปจนถึง**จับคู่เสื้อผ้าเป็นลุค**
ต่อยอดจากระบบแอดมิน + คลังข้อมูลที่มีอยู่แล้ว (ไม่รื้อของเดิม)

> เอกสารนี้แยกจาก [planning.md](planning.md) (ซึ่งเป็นเฟส frontend หลัก) — โฟกัสเฉพาะความสามารถ AI

---

## 1. เป้าหมาย

ทำให้การลงสินค้า "โยนรูปดิบแล้วจบ" และให้แอปฉลาดพอจะ**แนะนำลุค**ได้เอง แบ่งเป็นความสามารถ 2 ก้อน:

1. **ผู้ช่วยลงสินค้าอัจฉริยะ** — รับรูปดิบ (มีพื้นหลัง) → ตัดพื้นหลัง + จัดหมวด + ตั้งชื่อ + ติดสไตล์ + เดาราคา → เข้า DOOD admin
2. **สไตลิสต์จับคู่ลุค** — อ่านทั้งคลัง (สไตล์/สี/หมวด) แล้วแมตช์หมวก+เสื้อ+กางเกงเป็นลุคสวย

> ก้อน 2 ต้องพึ่งก้อน 1 (คลังต้องมีสไตล์/ข้อมูลครบก่อน AI ถึงแมตช์เก่ง) → ทำเรียงกัน

---

## 2. หลักการ (ยึดตลอด)

- **ต่อยอด ไม่รื้อ** — ใช้ admin API / products.json / styles.ts / catalogSource ที่มีอยู่
- **AI เสนอ คนเคาะ** — โดยเฉพาะราคา/ลิงก์ AI เดาเป็น "ค่าเริ่มต้น" ให้ แต่คนกดยืนยันเสมอ
- **เฟสละใช้งานได้จริง** — จบเฟสไหนก็ได้ประโยชน์ทันที ไม่ต้องรอครบ
- **จุดสลับ backend เดิมไม่พัง** — ยังคง catalogSource (อ่าน) / admin api (เขียน) เป็นจุดเดียว
- **ความลับไม่หลุด** — API key อยู่ฝั่งเซิร์ฟเวอร์เท่านั้น ไม่โผล่ในโค้ด frontend / ไม่ขึ้น git

---

## 3. การตัดสินใจที่ล็อกแล้ว (จากการคุย)

| เรื่อง | สรุป |
|---|---|
| เครื่องตัดพื้นหลัง | **@imgly/background-removal-node** — รันในเครื่อง ฟรี ไม่ต้องมี key |
| โมเดล AI | **Claude Haiku 4.5** เป็นหลัก (ถูก+เร็ว+พอ), ขยับ Sonnet 5 เฉพาะงานแมตช์ที่อยากละเอียด |
| ราคา/ลิงก์ซื้อ | AI เดาเป็นค่าแนะนำได้ แต่ **คนต้องยืนยัน** (รูปบอกราคาจริงไม่ได้) |
| ขอบเขต | อยากได้ครบ: จัดหมวด + กรอกข้อมูล + แมตช์ลุค (fashion stylist) |

---

## 4. สถาปัตยกรรมรวม (เชื่อมกับของเดิมยังไง)

```
รูปดิบ (มีพื้นหลัง)
   │
   ├─[เฟส 1]→ @imgly ตัดพื้นหลัง ──┐
   │                               ├─→ sharp trim + สีเฉลี่ย + WebP  (มีอยู่แล้วใน admin-plugin)
   ├─[เฟส 2]→ Claude วิชัน วิเคราะห์ ┘        │
   │          {หมวด, ชื่อ, สไตล์, ราคาแนะนำ}  │
   │                                          ▼
   │                                  POST /api/products  (admin API เดิม)
   │                                          │
   │                                  catalog/products.json  (คลังเดียว)
   │                                          │
   └─[เฟส 3]→ Claude อ่านคลัง → แมตช์ลุค → เสนอใน DOOD (แอปหลัก)
```

จุดต่อของใหม่:
- เฟส 1: แก้ `processImage()` ใน [scripts/admin-plugin.mjs](scripts/admin-plugin.mjs) + [scripts/add-images.mjs](scripts/add-images.mjs)
- เฟส 2: เพิ่ม endpoint `POST /api/analyze` (เซิร์ฟเวอร์เรียก Claude) + ปุ่ม "อัปโหลดอัจฉริยะ" ใน [src/admin/AdminApp.tsx](src/admin/AdminApp.tsx)
- เฟส 3: เพิ่ม endpoint `POST /api/stylist` + ปุ่ม "AI จัดลุค" ในแอปหลัก ([src/App.tsx](src/App.tsx))

---

## 5. เฟส 1 — ตัดพื้นหลังอัตโนมัติ  (ฟรี · ไม่ต้องมี key)

**ได้อะไร:** โยนรูปดิบ (พื้นหลังอะไรก็ได้) เข้าหน้าแอดมินหรือโฟลเดอร์ catalog → ระบบตัดพื้นหลังให้เอง ไม่ต้องไปตัดที่ remove.bg/Canva ก่อน

**ทำอะไร:**
- เพิ่ม dependency: `@imgly/background-removal-node` (โหลดโมเดล ~40MB ครั้งแรก)
- แก้ `processImage()` — เพิ่มสเต็ป "ตัดพื้นหลัง" ก่อน trim:
  - ตรวจก่อน: ถ้ารูป**โปร่งอยู่แล้ว** (มี alpha และขอบโปร่ง) → ข้าม (ไม่เสียเวลา/ไม่ทำพัง)
  - ถ้า**ทึบ/มีพื้นหลัง** → รัน @imgly ตัดออก → ได้ PNG โปร่ง → ส่งต่อเข้า trim/สี/WebP เดิม
- แก้ `add-images.mjs` เหมือนกัน (ให้ทาง bulk ได้ประโยชน์ด้วย)
- อัปเดตข้อความในหน้าแอดมิน: "ลากรูปมาได้เลย มีพื้นหลังก็ได้ ระบบตัดให้"

**ข้อควรรู้:**
- @imgly เนียนกับรูปเสื้อผ้าทั่วไป แต่ขอบเส้นผม/ตาข่ายบาง ๆ อาจไม่เป๊ะเท่า remove.bg — เผื่อปุ่ม "ตัดพื้นหลังไหม" (เปิด/ปิด) ในฟอร์มไว้ override

**เกณฑ์ผ่านเฟส 1:** ✅ เสร็จแล้ว (2026-07-26)
- [x] อัปโหลดรูปมีพื้นหลัง → ได้สินค้าพื้นโปร่งในแอป โดยไม่ต้องแต่งรูปเอง (ทดสอบ: พื้นเทา → ตัดออก 33% โปร่ง, 1.0s)
- [x] อัปโหลดรูปโปร่งอยู่แล้ว → ข้ามการตัดอัตโนมัติ (0.1s ไม่ตัดซ้ำ)
- [x] `npm run add` ก็ตัดพื้นหลังให้เช่นกัน (แชร์ util เดียวกัน)
- [x] build/typecheck ผ่าน + รูปในแอปยังคมชัด
- [x] แก้เรื่อง sharp/libvips ชนกัน → แยก @imgly ไปรัน child process (worker)
- [x] checkbox "ตัดพื้นหลังให้อัตโนมัติ" ในแอดมิน + รองรับ .jpg แล้ว

> **สถาปัตยกรรมที่ได้:** [scripts/remove-bg.mjs](scripts/remove-bg.mjs) (worker) + [scripts/bg-util.mjs](scripts/bg-util.mjs) (isCutout/maybeRemoveBg) ใช้ร่วมกันทั้ง admin-plugin และ add-images

---

## 6. เฟส 2 — AI กรอกข้อมูลให้  (ต้องมี API key)

**ได้อะไร:** อัปโหลดรูป → Claude วิชันดูรูปแล้ว**เดา หมวด + ชื่อ (ไทย) + สไตล์ + ราคาแนะนำ** → ฟอร์มแอดมินเติมมาให้ครบ → คนแค่ตรวจ/แก้ราคาแล้วกดบันทึก

> **อัปเดตจากรอบทดลอง (2026-07-28) — เลื่อนไปทำเฟสหลัง ยังทดลองระบบอยู่**
> เคาะรายละเอียดไว้แล้ว หยิบมาทำได้เลยตอนพร้อม:
> - **กรอกทุกฟิลด์ ยกเว้น ราคา + ลิงก์** (2 อันนี้คนกรอกเองเสมอ)
> - ฟิลด์ที่ AI กรอก: `category`, `fit` (long/short เฉพาะ pants), `gender`, `name` (ไทย), `style` (เลือกจาก 6 id จริง) — *สี+aspect auto อยู่แล้ว ไม่ใช้ AI*
> - **ทริกเกอร์: ปุ่ม "✨ ให้ AI กรอก"** (ไม่ auto ตอนอัปโหลด — คุมค่าใช้จ่าย + กดซ้ำได้)
> - **โมเดล: Haiku 4.5** (`claude-haiku-4-5-20251001`) ~0.03 บาท/รูป · ย่อรูป ≤768px + JPEG ก่อนส่ง
> - JSON ไม่มี `priceSuggest` แล้ว (ราคาคนกรอกเอง) · endpoint `POST /api/analyze` อ่าน key จาก `.env`
> - โค้ดร่างไว้ครั้งนึงแล้วถอยออก (คอมมิตไม่มี) — โครงพร้อมประกอบใหม่เร็ว

**ทำอะไร:**
- เพิ่ม endpoint `POST /api/analyze` ใน admin-plugin (ฝั่ง Node):
  - รับ base64 รูป → เรียก Anthropic Messages API (Haiku 4.5) พร้อมรูป + คำสั่งให้ตอบ **JSON เท่านั้น**
  - โครง JSON: `{ category: 'hat'|'top'|'pants', name: string, style: <id จาก styles.ts>, priceSuggest: number, confidence: number }`
  - ส่งรายการ style id ที่มีจริง ([src/config/styles.ts](src/config/styles.ts)) เข้าไปใน prompt เพื่อบังคับให้เลือกจากของที่มี
- แก้หน้าแอดมิน: ปุ่ม/โหมด **"อัปโหลดอัจฉริยะ"**
  - เลือกรูป → เรียก `/api/analyze` → ฟอร์มเติม หมวด/ชื่อ/สไตล์/ราคา อัตโนมัติ (มีป้าย "AI แนะนำ")
  - คนแก้ได้ทุกช่อง โดยเฉพาะ**ราคา** (เน้นให้ยืนยัน) แล้วกดบันทึก (ไหลเข้า flow เดิม = ตัดพื้นหลัง + trim + save)
- รองรับ **อัปโหลดหลายรูปรวด** (คิว) → ทยอยวิเคราะห์ + ใส่ทีละตัว

**การจัดการ key อย่างปลอดภัย:**
- เก็บใน `.env` (เพิ่มใน `.gitignore`) → อ่านด้วย `process.env.ANTHROPIC_API_KEY` ฝั่ง admin-plugin เท่านั้น
- **ห้าม**ส่ง key ไป frontend — frontend เรียกแค่ `/api/analyze` ของเราเอง เซิร์ฟเวอร์เป็นตัวถือ key คุยกับ Anthropic
- ถ้าไม่ตั้ง key → ปุ่มอัจฉริยะ disabled + ขึ้นข้อความ (แอปยังใช้แบบกรอกมือได้ตามปกติ)

**เกณฑ์ผ่านเฟส 2:**
- [ ] อัปโหลดรูปเสื้อ → ฟอร์มเดาหมวด "เสื้อ" + ตั้งชื่อไทยสมเหตุผล + เลือกสไตล์จากลิสต์จริง + ใส่ราคาแนะนำ
- [ ] แก้ราคาแล้วบันทึกได้ปกติ, สินค้าเข้า products.json ครบ field
- [ ] ไม่มี key → ระบบไม่พัง (แค่ปิดฟีเจอร์อัจฉริยะ)
- [ ] key ไม่โผล่ใน bundle frontend / ไม่ขึ้น git

---

## 7. เฟส 3 — AI สไตลิสต์จับคู่ลุค  (ต้องมี API key)

**ได้อะไร:** ปุ่ม **"AI จัดลุคให้"** — แทนที่จะสุ่มมั่ว AI อ่านคลังแล้วจับ หมวก+เสื้อ+กางเกง ให้เข้ากันตามสี/สไตล์ พร้อมเหตุผลสั้น ๆ

**ทำอะไร:**
- เพิ่ม endpoint `POST /api/stylist`:
  - ส่ง metadata คลังแบบกระชับ (id, category, name, style, color ต่อชิ้น) ให้ Claude
  - ขอกลับเป็นลุค N ชุด: `{ hat, top, pants: <id>, vibe: string }[]` + บังคับเลือกจาก id ที่มีจริง
  - เซิร์ฟเวอร์ตรวจ id ว่ามีจริงก่อนส่งต่อ (กัน AI มั่ว)
- ฝั่งแอป:
  - ปุ่ม "AI จัดลุค" (ข้าง 🎲) → เรียก stylist → ได้ลุคแนะนำ
  - ทางเลือกแสดงผล: (ก) ใส่ลุคแรกขึ้นมาสคอตเลย + ปัดดูลุคถัด ๆ ไป หรือ (ข) ชั้นวาง "ลุคแนะนำ" ให้เลือก → กดใส่ (reuse applyLook/คอลเลกชันที่มีอยู่)
- ต่อยอด: ใช้สไตล์ที่ผู้ใช้ล็อก/ใส่อยู่เป็น seed ("จัดลุครอบ ๆ เสื้อตัวนี้")

**ข้อควรรู้:**
- ต้องมีสินค้าในคลังพอ (โดยเฉพาะติด **สไตล์** ครบ — ได้จากเฟส 2) AI ถึงแมตช์ได้ดี
- เริ่มด้วย Haiku ก่อน ถ้าอยากได้รสนิยมละเอียดขึ้นค่อยลอง Sonnet เฉพาะ endpoint นี้

**เกณฑ์ผ่านเฟส 3:**
- [ ] กด "AI จัดลุค" → ได้ลุคที่ทุกชิ้นมีจริงในคลัง + ดูเข้ากัน
- [ ] ใส่ลุคแนะนำขึ้นมาสคอตได้ (reuse ระบบเดิม) + บันทึกลงคอลเลกชันได้
- [ ] id ที่ AI ตอบผิด/ไม่มีจริง ถูกกรองทิ้ง ไม่ทำแอปพัง

---

## 8. ความจริง / ข้อจำกัด (พูดตรง)

- **ราคา + ลิงก์ซื้อ**: AI เดาได้แค่ "ค่าแนะนำ" คนต้องยืนยัน — รูปไม่ได้บอกราคาจริง
- **จับขนาดให้เข้ามาสคอตเป๊ะ 100%**: ทำอัตโนมัติเต็มรูปยาก → พึ่ง trim + ตัวคูณสัดส่วนต่อหมวดเหมือนเดิม + ถ่ายรูปในหมวดสเกลใกล้กัน
- **คุณภาพตัดพื้นหลัง**: @imgly ดีกับเสื้อผ้าทั่วไป แต่ไม่เท่าบริการเสียเงินกับขอบยาก ๆ → มีปุ่ม override ไว้
- **เฟส 2/3 ต้องออนไลน์ + มี key**: ถ้าเน็ตหลุด/ไม่มี key → ปิดฟีเจอร์ AI แต่แอป+แอดมินแบบมือยังทำงานปกติ

---

## 9. ต้นทุน (โดยประมาณ)

| รายการ | ต้นทุน |
|---|---|
| ตัดพื้นหลัง (@imgly) | ฟรี รันในเครื่อง |
| เติมเครดิต Anthropic ขั้นต่ำ | ~$5 (~175 บาท) ครั้งเดียว ใช้ได้ยาว |
| เดาข้อมูล 1 รูป (Haiku) | ~$0.002 (~0.07 บาท) → ลง 100 ชิ้น ≈ 7 บาท |
| จัดลุค 1 ครั้ง (Haiku) | ~$0.007 (~0.25 บาท) → 1,000 ครั้ง < 10 บาท |

> ที่สเกลร้านทั่วไป ค่า API แทบเป็นศูนย์ — ต้นทุนจริงคือแค่ต้องมีบัญชี + เติมเครดิตขั้นต่ำ

---

## 10. ลำดับลงมือ (ทำทีละเฟส ทดสอบทุกเฟส)

1. **เฟส 1** — เสียบ @imgly เข้า pipeline (admin + add-images) + ปุ่ม override + ทดสอบรูปมีพื้นหลัง/โปร่ง
2. **เฟส 2** — `.env` + `/api/analyze` (Haiku วิชัน) + โหมดอัปโหลดอัจฉริยะในแอดมิน + คิวหลายรูป
3. **เฟส 3** — `/api/stylist` + ปุ่ม "AI จัดลุค" ในแอป + ผูกกับ applyLook/คอลเลกชัน

> เริ่มเฟส 1 ได้ทันทีโดยไม่ต้องมี key — เฟส 2/3 ค่อยจัดการ key ตอนถึง

---

## 11. งานที่ต้องเตรียม (ฝั่งผู้ใช้)

- [ ] เฟส 1: ไม่ต้องเตรียมอะไร
- [ ] เฟส 2/3: สมัคร Anthropic API + เติมเครดิต ~$5 + เอา key มาใส่ `.env` (ผมจะแนะนำวิธีตอนถึง)

---

## 12. วิสัยทัศน์ระยะยาว — "ทีม AI ประจำร้าน" (เก็บไว้ทำอนาคต)

เป้าปลายทางของเจ้าของ: มีผู้ช่วย AI เหมือนทีมงานส่วนตัว 5 บทบาท แบ่งเป็น 2 กลุ่ม

**กลุ่ม A — ฟีเจอร์ในตัวสินค้า (ลูกค้า/แอดมินใช้ · อยู่ใน DOOD)**
1. **พนักงานตัดรูป + ลงเว็บ** → เฟส 1-2 ของเอกสารนี้
2. **สไตลิสต์จัดชุด** → เฟส 3 ของเอกสารนี้

**กลุ่ม B — ผู้ช่วยส่วนตัวของเจ้าของร้าน (เครื่องมือของเจ้าของ ไม่ใช่ฟีเจอร์ลูกค้า)**
| # | บทบาท | AI ทำได้แค่ไหน | ต้องมีอะไรก่อน |
|---|---|---|---|
| 3 | เลขาบอกว่าต้องทำอะไร | จัดลำดับ/เตือน/แนะนำ — ฉลาดเท่าข้อมูลที่ป้อน | ข้อมูลจริง (งาน/ยอดขาย) |
| 4 | วางแผนการตลาด ดึงลูกค้า | ระดมไอเดีย/ทำคอนเทนต์เก่ง แต่ดึงลูกค้าเองไม่ได้ | เว็บ live + ทราฟฟิก + งบ |
| 5 | เช็คระบบเว็บมีปัญหาไหม | เทสอัตโนมัติ/แจ้ง error/uptime | เว็บที่ deploy จริง |

**ความจริงที่ต้องยึด:**
- กลุ่ม B จะ "เก่งจริง" ก็ต่อเมื่อมีเว็บ live + ผู้ใช้จริง + ข้อมูลจริง → **จุดปลดล็อกคือ backend + launch**
- กับดักที่ต้องเลี่ยง: สร้างทีม AI ครบก่อนมีสินค้า live = ลงแรงกับของที่ยังไม่มีเชื้อเพลิง

**เส้นทาง leverage สูงสุด:**
1. ทำ AI กลุ่ม A ต่อ (เฟส 1 → 2 → 3) — ได้ของจริงใช้ได้เลย
2. **backend + launch** — ปลดล็อกให้กลุ่ม B มีเชื้อเพลิง
3. หลัง launch ค่อยประกอบผู้ช่วยกลุ่ม B ทีละตัวบนข้อมูลจริง

> ระหว่างนี้ บทบาท 3/4/5 ทำแบบ "ชวนคุยกับ AI" ได้เลยโดยไม่ต้องสร้างระบบ (ระดมไอเดียการตลาด, จัดลำดับงาน, รันเทสเช็คระบบ)
